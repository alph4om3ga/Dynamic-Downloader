diff --git a/JudasEncodingManager/JudasEncodingManager/Build.ps1 b/JudasEncodingManager/JudasEncodingManager/Build.ps1
index 818a0b4..f38096a 100644
--- a/JudasEncodingManager/JudasEncodingManager/Build.ps1
+++ b/JudasEncodingManager/JudasEncodingManager/Build.ps1
@@ -24,7 +24,7 @@ function ConvertTo-ReleaseVersion {
         '^[vV]?(?<major>\d+)\.(?<minor>\d+)(?:\.(?<patch>\d+))?(?:\.\d+)?$'
     )
     if (-not $match.Success) {
-        throw "Invalid $Source version '$Version'. Expected a version such as 1.3.1 or v1.3.1."
+        throw "Invalid $Source version '$Version'. Expected a version such as 1.3 or v1.3."
     }
 
     $patch = if ($match.Groups["patch"].Success) {
@@ -46,6 +46,7 @@ $ScriptDir = Split-Path -Parent $MyInvocation.MyCommand.Path
 Set-Location $ScriptDir
 
 $projectPath = Join-Path $ScriptDir "JudasEncodingManager.csproj"
+$regressionProjectPath = Join-Path $ScriptDir "..\RegressionTests\JudasEncodingManager.RegressionTests.csproj"
 
 if ($ExpectedVersion) {
     Write-Host "[release] Validating release version..." -ForegroundColor Yellow
@@ -77,13 +78,15 @@ if ($Clean) {
 
 # Restore packages
 Write-Host "[2/5] Restoring NuGet packages..." -ForegroundColor Yellow
-dotnet restore
-if ($LASTEXITCODE -ne 0) { throw "Restore failed" }
+dotnet restore $projectPath
+if ($LASTEXITCODE -ne 0) { throw "Application restore failed" }
+dotnet restore $regressionProjectPath
+if ($LASTEXITCODE -ne 0) { throw "Regression restore failed" }
 Write-Host "      Restored!" -ForegroundColor Green
 
 # Run focused safety checks before producing a release build.
 Write-Host "[3/5] Running regression checks..." -ForegroundColor Yellow
-dotnet run --project "..\RegressionTests\JudasEncodingManager.RegressionTests.csproj" --framework net8.0-windows
+dotnet run --project $regressionProjectPath --framework net8.0-windows --no-restore
 if ($LASTEXITCODE -ne 0) { throw "Regression checks failed" }
 Write-Host "      Passed!" -ForegroundColor Green
 
diff --git a/JudasEncodingManager/JudasEncodingManager/JudasEncodingManager.csproj b/JudasEncodingManager/JudasEncodingManager/JudasEncodingManager.csproj
index 1d3633c..3236a9e 100644
--- a/JudasEncodingManager/JudasEncodingManager/JudasEncodingManager.csproj
+++ b/JudasEncodingManager/JudasEncodingManager/JudasEncodingManager.csproj
@@ -10,7 +10,7 @@
 
     <AssemblyName>JudasEncodingManager</AssemblyName>
     <RootNamespace>JudasEncodingManager</RootNamespace>
-    <Version>1.3.1</Version>
+    <Version>1.3</Version>
     <Authors>Judas Team</Authors>
     <Description>Encoding Pipeline Manager for Judas</Description>
     <Company>Judas</Company>
